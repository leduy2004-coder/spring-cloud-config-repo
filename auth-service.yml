server:
  port: 8081


spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  cache:
    type: redis

  data:
    mongodb:
      uri: mongodb://root:abc123!%40%23@localhost:27017/auth-service?authSource=admin
    redis:
      host: localhost
      port: 6379
      password: myredispass
      repositories:
        enabled: false
      ssl:
        enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    open-in-view: false

  application:
    security:
      jwt:
        secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
        expiration: 3600000     # 1 h in milliseconds
        refresh-token:
          expiration: 604800000 # 7 days in milliseconds
  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: http://localhost:8080/issuer